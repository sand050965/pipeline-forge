pipeline:
  name: valid-linear-chain
  description: Valid linear dependency chain

stages:
  - test

job1:
  - stage: test
  - image: alpine:latest
  - script: echo "job1"

job2:
  - stage: test
  - image: alpine:latest
  - script: echo "job2"
  - needs: [ job1 ]

job3:
  - stage: test
  - image: alpine:latest
  - script: echo "job3"
  - needs: [ job2 ]

job4:
  - stage: test
  - image: alpine:latest
  - script: echo "job4"
  - needs: [ job3 ]