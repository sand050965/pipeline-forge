pipeline:
  name: valid-basic
  description: Basic valid pipeline

stages:
  - build
  - test

build-job:
  - stage: build
  - image: alpine:latest
  - script: echo "building"

test-job:
  - stage: test
  - image: alpine:latest
  - script:
      - echo "testing"
      - echo "done"