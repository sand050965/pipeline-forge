pipeline:
  name: undefined-stage

stages:
  - build

test-job:
  - stage: nonexistent
  - image: alpine:latest
  - script: echo "testing"

build-job:
  - stage: build
  - image: alpine:latest
  - script: echo "building"