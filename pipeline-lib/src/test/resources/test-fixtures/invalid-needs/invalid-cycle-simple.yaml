pipeline:
  name: cycle-simple
  description: Simple circular dependency (2 jobs)

stages:
  - test

job1:
  - stage: test
  - image: alpine:latest
  - script: echo "job1"
  - needs: [ job2 ]

job2:
  - stage: test
  - image: alpine:latest
  - script: echo "job2"
  - needs: [ job1 ]