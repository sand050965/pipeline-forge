apiVersion: v1
kind: Service
metadata:
  name: execution-service
  labels:
    app: execution-service
    {{- include "cicd-system.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: execution-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
